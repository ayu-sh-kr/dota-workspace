/** Default visual slot for the light-DOM wrapper rendered by `dota-slide`. */
export const DotaSlideStyle = {
  container: "w-full h-full",
};

/** Per-instance replacement for the `dota-slide` wrapper class. */
export interface DotaSlideStyleConfig {
  container?: string;
}
